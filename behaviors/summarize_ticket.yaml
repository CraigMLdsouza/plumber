name: summarize_ticket

model:
  provider: ollama
  name: phi3:mini
  temperature: 0.1

input: {schema: {ticket_text: string}}
output:
  schema:
    summary: string
    priority: {type: enum, values: [low, medium, high]}
validation:
  max_length: {summary: 120}
