name: array_min_items
model: {provider: gemini, name: gemini-2.5-flash}
input: {schema: {text: string}}
output:
  schema:
    participants: {type: array, items: string}
validation:
  min_items: {participants: 1}
